Simple script allows you download files on local machine from torrent (via aria2c) and send it via TelegramBotAPI (use telebot lib)

Currently telegram allow send only 50Mb files via Bot api, so files splits
